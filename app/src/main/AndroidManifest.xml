<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fastcampus.aop.part2.mgr_villa">

    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mgr_villa"
        android:name=".sharedPreferences.MyApplication"
        >
        <activity
        android:name=".MainActivity"
        android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignUpActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".VillaHomeActivity"/>

        <activity android:name=".LoginActivity"/>

        <activity android:name=".SearchIdActivity"/>

        <activity android:name=".SearchIdCompleteActivity"/>

        <activity android:name=".SearchPwActivity"/>

        <activity android:name=".ChangePwActivity"/>

        <activity android:name=".ChoiceMgrTenantActivity"/>

        <activity android:name=".VillaInfoActivity"/>

        <activity android:name=".AddressSearchActivity"/>

        <activity android:name=".NoticeListActivity"/>

        <activity android:name=".NoticeActivity"/>

        <activity android:name=".MgrCostDivActivity"/>

        <activity android:name=".VillaMgrAccountsListActivity"/>

        <activity android:name=".MgrStandardCostActivity"/>

        <activity android:name=".TenantListActivity"/>

        <activity android:name=".AddAccountActivity"/>

        <activity android:name=".TenantCostListActivity"/>

        <activity android:name=".TenantRoomCostForMGRActivity"/>

        <activity android:name=".TenantInOutVillaActivity"/>

        <activity android:name=".RequestTenantActivity"/>

        <activity android:name=".AddressSearchForTenantActivity"/>

    </application>

</manifest>